services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: tenderradar
      POSTGRES_PASSWORD: tenderradar
      POSTGRES_DB: tenderradar
    ports:
      - "5432:5432"
    volumes:
      - tenderradar_pg:/var/lib/postgresql/data

volumes:
  tenderradar_pg: